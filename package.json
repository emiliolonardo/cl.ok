{
  "name": "dos-time-os",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "pnpm --filter @dos/web dev --port 5466",
    "dev:default-port": "pnpm --filter @dos/web dev",
    "build": "pnpm --filter @dos/web build",
    "lint": "pnpm --filter @dos/web lint"
  }
}
